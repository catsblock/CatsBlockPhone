package com.catsblock.phone

import android.app.Application

/**
 * Classe principal de Application do CatsBlock Phone.
 * Inicializa componentes globais e banco de dados local.
 */
class CatsBlockApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Inicialização de serviços e configurações do aplicativo
    }
}
